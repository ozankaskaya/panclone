import type { IdentifierOption } from './types';
interface TransformParams {
    source: string;
    filePath: string;
    rootPath: string;
    packageName: string;
    identOption: IdentifierOption;
}
export declare const transformSync: ({ source, filePath, rootPath, packageName, identOption, }: TransformParams) => string;
export declare const transform: ({ source, filePath, rootPath, packageName, identOption, }: TransformParams) => Promise<string>;
export {};
