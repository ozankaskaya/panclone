Object.defineProperty(exports, '__esModule', { value: true });

var wrapperUtils = require('./wrapperUtils.js');

/**
 * Create a wrapped version of the user's exported `getInitialProps` function
 *
 * @param origGetInitialProps The user's `getInitialProps` function
 * @param parameterizedRoute The page's parameterized route
 * @returns A wrapped version of the function
 */
function withSentryGetInitialProps(
  origGetInitialProps,
  parameterizedRoute,
) {
  return async function (
    ...getInitialPropsArguments
  ) {
    return wrapperUtils.callDataFetcherTraced(origGetInitialProps, getInitialPropsArguments, {
      parameterizedRoute,
      dataFetchingMethodName: 'getInitialProps',
    });
  };
}

exports.withSentryGetInitialProps = withSentryGetInitialProps;
//# sourceMappingURL=withSentryGetInitialProps.js.map
