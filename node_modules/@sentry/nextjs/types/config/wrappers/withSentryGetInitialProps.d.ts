import { NextPage } from 'next';
declare type GetInitialProps = Required<NextPage<unknown>>['getInitialProps'];
/**
 * Create a wrapped version of the user's exported `getInitialProps` function
 *
 * @param origGetInitialProps The user's `getInitialProps` function
 * @param parameterizedRoute The page's parameterized route
 * @returns A wrapped version of the function
 */
export declare function withSentryGetInitialProps(origGetInitialProps: GetInitialProps, parameterizedRoute: string): GetInitialProps;
export {};
//# sourceMappingURL=withSentryGetInitialProps.d.ts.map